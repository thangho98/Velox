{
  "meta": { "schema_version": "1.1.0", "awf_version": "3.3.0" },
  "project": { "name": "Velox", "type": "Media Server", "status": "active" },
  "tech_stack": { 
      "frontend": { "web": "React", "android": "Jetpack Compose" }, 
      "backend": { "lang": "Go", "framework": "Standard Library / Custom" }, 
      "database": { "main": "SQLite / Postgres?" } 
  },
  "database_schema": { "tables": ["media_markers", "users", "media_files"], "relationships": ["One media_file has many media_markers"] },
  "api_endpoints": [
      { "path": "/api/admin/markers/stats", "method": "GET", "explained": "Get statistics on intro/credit markers for admin dashboard." },
      { "path": "/api/admin/markers/backfill", "method": "POST", "explained": "Trigger background audio fingerprint detection for a library." }
  ],
  "business_rules": [
      "Marker detection triggers a background task and broadcasts progress to admins via websocket.",
      "Admin features like backfilling markers are guarded by admin checks in both frontend apps.",
      "User subjective preferences for auto-skipping segments belong in general user preferences."
  ],
  "features": [
      "Skip Intro / Credits",
      "Media Scanner",
      "Live Transcoding",
      "User Management"
  ],
  "knowledge_items": { 
      "patterns": ["Use ViewModel to bind Jetpack Compose state."], 
      "gotchas": ["Android app lacks websocket implementation; polling/fire-and-forget is required for background tasks for now."], 
      "conventions": ["Use Netflix theme colors (NetflixRed, NetflixWhite, NetflixDarkGray) across Android app UI."] 
  }
}
