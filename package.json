{
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.1.0"
  },
  "lint-staged": {
    "webapp/src/**/*.{ts,tsx}": [
      "webapp/node_modules/.bin/prettier --write --config webapp/.prettierrc"
    ],
    "backend/**/*.go": [
      "gofmt -w"
    ]
  }
}
