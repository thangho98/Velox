# Maestro config for Velox Mobile
# Docs: https://maestro.mobile.dev

appId: com.velox.app
name: Velox Mobile Tests

# Default timeout for assertions (ms)
ext:
  SERVER_URL: "http://10.0.2.2:8080"  # Android emulator → host machine
  USERNAME: "admin"
  PASSWORD: "admin123"
